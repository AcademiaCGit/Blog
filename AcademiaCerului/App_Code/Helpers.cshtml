@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<AcademiaCerului.Core.Objects.Tag> tags) { 
foreach(var tag in tags)
{
        <div class="tags-div">
            @AcademiaCerului.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}}